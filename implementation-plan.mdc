# AI MVP BYOK - Implementation Plan

> 约定：每完成一步，在对应条目下追加 `Done` 与两行记录。

---

## Phase 1: 基础结构搭建

### 1.1 新增流程文件 `implementation-plan.mdc`

- [x] 在仓库根目录创建本文件，用于记录后续每步进展。

Done:
创建 `implementation-plan.mdc`，定义 Phase 1-5 的结构化进度追踪。

### 1.2 创建 `packages/ai` package 骨架

- [x] 新建 `packages/ai/package.json`
- [x] 新建 `packages/ai/tsconfig.json`
- [x] 新建 `packages/ai/CHANGELOG.md`
- [x] 新建 `packages/ai/src/index.ts`
- [x] 创建子目录：`providers/`, `credentials/`, `prompts/`, `rag/`, `usage/`, `graph/`
- [x] 新建 `packages/ai/src/schemas.ts`

Done:
创建 `@folionote/ai` package，包含 package.json、tsconfig.json、CHANGELOG.md 与完整目录结构。

### 1.3 落地最小可复用的类型与约束（@folionote/ai）

- [x] Provider 枚举与能力声明 (`providers/types.ts`)
- [x] Usage 类型结构 (`usage/types.ts`)
- [x] Prompt version 常量 (`prompts/versions.ts`)
- [x] Credentials 抽象接口占位 (`credentials/types.ts`)
- [x] RAG 抽象接口占位 (`rag/types.ts`)
- [x] Graph 抽象接口占位 (`graph/types.ts`)

Done:
定义 AI_PROVIDERS 枚举、ProviderConfig、TokenUsage、AiRun、TextChunk、GraphNode 等核心类型与接口。

### 1.4 在 `@folionote/api` 增加最小接入点

- [x] 更新 `packages/api/package.json` 添加 `@folionote/ai` 依赖
- [x] 新建 `packages/api/src/routers/ai.ts`
- [x] 更新 `packages/api/src/routers/index.ts` 挂载 `aiRouter`

Done:
在 `@folionote/api` 添加 `@folionote/ai` 依赖，创建 aiRouter 并挂载到 appRouter。

### 1.5 更新文档与配置

- [x] 更新 `docs/ai-mvp-byok-v0.md` 第 5 节目录规划
- [x] 更新 `turbo.json` 透传 AI 相关环境变量

Done:
重写 Section 5，分为 AI 能力层（packages/ai）与 API 网关层；turbo.json 仅透传 `AI_KEY_ENCRYPTION_SECRET`，并新增 `apps/server/config.toml` 作为成本估算配置载体。

### 1.6 自动化校验

- [x] 运行 `pnpm check` 确保无 lint 错误
- [x] 运行 `pnpm check-types` 确保无类型错误

Done:
运行 `pnpm check` 与 `pnpm check-types`，确保新增 package / router 通过 lint 与类型检查。
修复 `@folionote/ai` 的 export 冲突与 Vercel AI SDK 相关类型标注问题。

### 1.7 接入 Vercel AI SDK（@folionote/ai）

- [x] 为 `@folionote/ai` 添加依赖：`ai`、`@ai-sdk/openai`、`@ai-sdk/anthropic`、`@ai-sdk/google`、`@ai-sdk/provider`
- [x] 新增 `packages/ai/src/vercel-ai.ts`，提供 chat / embedding model 工厂（不导出到 `src/index.ts`）
- [x] 新增单元测试 `packages/ai/__tests__/vercel-ai.test.ts`

Done:
为 `@folionote/ai` 引入 Vercel AI SDK 依赖，并新增 `vercel-ai.ts` 作为模型工厂入口（不在 index 导出）。
补齐 `vercel-ai` 单测，覆盖 chat/embedding 工厂的基础行为。

### 1.8 调整 Gemini 默认模型与可用性提示

- [x] 将 Gemini 默认 chat 模型更新为 `gemini-2.5-flash-lite`
- [x] 默认使用 Gemini OpenAI compatible baseUrl，并在 API 侧对地区不可用错误给出可操作提示
- [x] Web 端 AI Demo 增加 baseUrl 可选输入，便于使用代理 / 网关

Done:
将 Gemini 默认 chat 模型更新为 `gemini-2.5-flash-lite`，并将默认 baseUrl 切换为 OpenAI compatible 端点。
在 API 与 Web Demo 增加错误提示与 baseUrl 覆盖入口，避免地域限制时报 500 且无法配置。

### 1.9 重构 AI SDK Provider 工厂与模型选择组件

- [x] 在 Provider registry 中补充 “默认实例 / create 工厂” 两种接入方式的结构化元数据
- [x] 重构 `vercel-ai.ts`：对齐 AI SDK 文档风格，并补齐 Gemini embedding 工厂（native baseUrl）
- [x] Web 端 `ModelSelector` 支持按 Provider 分组，并内置常用模型列表

Done:
更新 `ProviderConfig` 以记录 AI SDK 两种接入方式，并为 Gemini/Qwen 增加可选 baseUrl 选项。
重构模型工厂与 Web 端模型选择器，覆盖 Gemini native 与 OpenAI compatible 场景（含 embedding 的限制说明）。

---

## Phase 2: BYOK 基础设施（后续实现）

> 待 Phase 1 完成后再展开

### 2.1 用户级模型启用开关（全类型 + 全站生效）

- [x] 在 `packages/db/src/schema/ai.ts` 新增 `user_ai_model_settings` 表
- [x] 更新 `packages/db/src/index.ts` 与 `packages/db/src/index.lazy.ts` 注册 schema
- [x] 运行 `pnpm run db:generate` 生成迁移文件
- [x] 在 `packages/api/package.json` 添加 `@folionote/model-list` 依赖
- [x] 在 `packages/api/src/routers/ai.ts` 新增 `ai.getModelCatalog` 与 `ai.setModelEnabled`
- [x] 在 `ai.generateText` 中增加模型启用状态校验
- [x] 新增 `apps/web/src/hooks/use-ai-model-catalog.ts`
- [x] 新增 `apps/web/src/components/ui/switch.tsx`
- [x] 更新 `apps/web/src/routes/_app/settings/models.tsx` 增加模型列表与开关
- [x] 更新 `apps/web/src/components/ai-elements/chat-input.tsx` 使用动态 catalog
- [x] 更新 `apps/web/src/components/ai-elements/model-selector.tsx` 使用动态 catalog
- [x] 更新 `apps/web/src/routes/_app/knowledge.tsx` 增加模型回退逻辑
- [x] 更新 i18n 资源文件（en-US / zh-CN / ja-JP）
- [x] 新增单测 `packages/api/__tests__/routers/ai-model-catalog.test.ts`
- [x] 更新 `docs/ai-mvp-byok-v0.md` Section 23

Done:
实现用户级模型启用开关功能，支持全类型（chat/embedding/image/tts/stt 等）与全站生效。
新增 `user_ai_model_settings` 表存储用户覆盖，API 层合并默认与用户设置，Web 端提供搜索+开关 UI。

### 2.2 ChatInput 附件整合

- [x] 设计 ChatInput 文件附件 props 与类型（`AttachedFile`，受控模式）
- [x] 新增 `apps/web/src/components/ai-elements/chat-input/file-attachment.tsx`
- [x] 更新 `apps/web/src/components/ai-elements/chat-input/types.ts` 新增 `AttachedFile` 与 props
- [x] 更新 `apps/web/src/components/ai-elements/chat-input/chat-input.tsx` 渲染文件与笔记附件
- [x] 更新 `apps/web/src/components/ai-elements/chat-input/index.ts` 导出新组件与类型
- [x] 更新 `apps/web/src/routes/_app/knowledge.tsx` 适配 prop 重命名
- [x] 更新 i18n 资源文件（en-US / zh-CN / ja-JP）新增 `image`、`attachment` key
- [x] 更新 `docs/ai-input-caret.md` 补充 ChatInput 附件使用说明
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
整合 ChatInput 附件能力，支持笔记附件与文件附件的受控渲染。
新增 `FileAttachment` 组件，与 `NoteAttachment` 保持一致的 UI 风格与交互模式。

### 2.3 会话上下文与 @mention 对齐

- [x] 为 `ChatMessage` 增加 `mentionTitles` 字段
- [x] 更新 `MessageBubble` 渲染用户消息时传递 `mentionTitles`
- [x] 前端请求改用 `UIMessage[]` 格式并在发送时捕获 mention 标题
- [x] 服务端使用 `convertToModelMessages` 转换消息格式
- [x] 新增 `buildKnowledgeChatSystemPrompt` 会话模式系统提示构造
- [x] 更新 `packages/ai/src/stream-text.ts` 支持 `ModelMessage[]`
- [x] 补充会话模式单测
- [x] 更新 `docs/ai-mvp-byok-v0.md` 新增 Section 25
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
对齐 @mention 渲染逻辑与会话上下文拼接。前端使用 `UIMessage[]` 格式携带完整消息历史，服务端通过 `convertToModelMessages` 转换后传递给模型。
新增 `buildKnowledgeChatSystemPrompt` 函数用于会话模式，避免 system prompt 重复包含用户输入。

### 2.4 Knowledge Chat 会话持久化与断线恢复

- [x] 新增 `apps/web/src/features/knowledge/utils.ts` 中的消息序列化/反序列化工具
- [x] 新增 chatId 生成与 localStorage 管理函数（`generateChatId`, `getOrCreateChatId`, `setChatId`, `clearChatId`）
- [x] 新增消息 localStorage 存取函数（`saveChatMessages`, `loadChatMessages`, `clearChatMessages`）
- [x] 更新 `apps/web/src/features/knowledge/index.ts` 导出新工具函数
- [x] 更新 `apps/web/src/hooks/use-stream-text.ts` 支持 `chatId` 与 `assistantMessageId` 参数
- [x] 更新 `apps/web/src/routes/_app/knowledge.tsx` 集成 chatId 生命周期与消息持久化
- [x] 新增 `apps/server/src/services/ai-chat-store.ts` 内存存储模块
- [x] 更新 `apps/server/src/routes/ai-stream.ts` 支持 chatId 持久化与 GET 接口
- [x] 新增 `apps/server/__tests__/ai-chat-store.test.ts` 单测（11 个测试用例）
- [x] 运行 `pnpm run check-types` 与 `pnpm check` 确保无错误

Done:
实现 Knowledge Chat 会话持久化功能。前端通过 localStorage 存储 chatId 与消息历史，刷新页面后可恢复对话。
服务端新增内存存储模块，支持按 userId + chatId 隔离消息，并提供 GET /api/ai/chat/:chatId 接口查询。
流式响应完成后自动持久化 assistant 消息，支持断线后客户端重新加载已完成消息。

### 2.5 AI SDK v6 消息持久化最佳实践对齐

- [x] 重构 `apps/server/src/services/ai-chat-store.ts` 为 `UIMessage[]` 统一格式
- [x] 新增 `createChat`, `loadChat`, `loadChatMessages`, `saveChat`, `deleteChat`, `listUserChats` API
- [x] 废弃旧的 `markStreamInProgress`, `completeStream`, `isStreamInProgress` 接口
- [x] 更新 `apps/server/__tests__/ai-chat-store.test.ts` 覆盖新 API（21 个测试用例）
- [x] 重构 `apps/server/src/routes/ai-stream.ts` 使用 `toUIMessageStreamResponse`
- [x] 使用 `createIdGenerator` 服务端生成 messageId
- [x] 在 `onFinish` 回调统一落库完整 `UIMessage[]`
- [x] 使用 `consumeStream` 确保断线后仍能完成持久化
- [x] 新增 POST `/api/ai/chat` 接口生成 chatId
- [x] 新增 `apps/web/src/hooks/use-knowledge-chat.ts` 封装 `useChat` hook
- [x] 使用 `DefaultChatTransport` + `prepareSendMessagesRequest` 自定义请求体
- [x] 更新 `apps/web/src/routes/_app/knowledge.tsx` 使用 `useKnowledgeChat`
- [x] 删除 `useStreamText` 的使用（保留文件供其他场景使用）
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
按照 AI SDK v6 消息持久化最佳实践全面重构。服务端使用 `toUIMessageStreamResponse` + `createIdGenerator` + `onFinish` + `consumeStream` 实现规范的持久化流程。
前端新增 `useKnowledgeChat` hook 封装 AI SDK 的 `useChat`，通过 `DefaultChatTransport` 自定义请求体携带 BYOK 凭证与 RAG 上下文。
消息 ID 由服务端统一生成，确保跨会话一致性；客户端不再直接持久化消息，改由服务端在流完成时统一落库。

### 2.6 Provider ID 单一来源与 Moonshot 补齐

- [x] 新增 `packages/constants/src/ai-providers.ts` 导出 `AI_PROVIDER_IDS` 与 `AiProviderId`
- [x] 更新 `packages/constants/src/index.ts` 导出新常量
- [x] 更新 `packages/ai/package.json` 添加 `@folionote/constants` 依赖
- [x] 重构 `packages/ai/src/providers/types.ts` 使用 `AI_PROVIDER_IDS` 生成 `AI_PROVIDERS`
- [x] 删除 `apps/server/src/routes/ai-stream.ts` 本地 `AI_PROVIDERS` 列表
- [x] 重构 `apps/web/src/lib/ai-provider-config.ts` 使用共享类型
- [x] 重构 `apps/web/src/features/knowledge/types.ts` 与 `utils.ts` 使用共享类型
- [x] 补齐 `packages/model-list/src/model-providers/index.ts` Moonshot 注册
- [x] 补齐 `packages/model-list/src/ai-models/index.ts` Moonshot 注册
- [x] 新增 provider 一致性单测
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
将 provider ID 类型集中到 `@folionote/constants`，各消费方从单一来源派生，新增 provider 时只需修改常量文件。
补齐 Moonshot 在 model-list 的注册，修正 iconSrc 为 kimi.svg。

### 2.7 用户级 Provider 启用开关

- [x] 在 `packages/db/src/schema/ai.ts` 新增 `user_ai_provider_settings` 表
- [x] 更新 `packages/db/src/index.ts` 与 `packages/db/src/index.lazy.ts` 注册并导出 schema
- [x] 在 `packages/api/src/routers/ai.ts` 新增 `checkProviderEnabled` 辅助函数
- [x] 在 `packages/api/src/routers/ai.ts` 更新 `checkModelEnabled` 先校验 provider enabled
- [x] 在 `packages/api/src/routers/ai.ts` 更新 `getModelCatalog` 合并用户 provider 覆盖
- [x] 在 `packages/api/src/routers/ai.ts` 新增 `setProviderEnabled` 接口
- [x] 在 `apps/web/src/hooks/use-ai-model-catalog.ts` 新增 `useSetProviderEnabled` mutation
- [x] 在 `apps/web/src/routes/_app/settings/models.tsx` provider 分组 header 新增开关
- [x] 更新 i18n 资源文件（en-US / zh-CN / ja-JP）新增 `toggleProvider` key
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
实现用户级 Provider 启用开关功能，关闭 provider 后其下所有模型被视为禁用，设置页模型列表隐藏该 provider 的模型。
新增 `user_ai_provider_settings` 表存储用户覆盖，API 层合并默认与用户设置，Web 端 provider header 带开关。

### 2.8 模型选择自动同步 Provider 与 baseUrl

- [x] 为模型选择回调补充 providerId 透传
- [x] Knowledge Chat 切换模型时同步切换 provider 与 baseUrl
- [x] 更新 BYOK 文档，说明模型切换的 Provider 同步规则

Done:
模型切换时同步更新 provider 与 baseUrl，确保模型调用落在正确的 Provider 配置。
更新选择器回调与知识聊天入口的状态同步逻辑，并补充文档说明。

### 2.9 OpenAI 兼容 Provider 使用 Chat Completions

- [x] 对 OpenAI 兼容 Provider 改用 `chat` 模型入口
- [x] Gemini OpenAI compatible baseUrl 改用 `chat` 模型入口
- [x] 更新 BYOK 文档，说明 OpenAI 兼容 Provider 的调用路径

Done:
OpenAI 兼容 Provider 默认使用 `/chat/completions`，避免 `/responses` 不支持导致 404。
更新模型工厂选择逻辑，并补充文档提示该调用路径。

### 2.10 Knowledge Chat 历史持久化与可视化

- [x] 在 `packages/db/src/schema/ai.ts` 新增 `ai_chat_sessions` 表
- [x] 更新 `packages/db/src/index.ts` 与 `packages/db/src/index.lazy.ts` 注册并导出 schema
- [x] 重构 `apps/server/src/services/ai-chat-store.ts` 使用 PostgreSQL 为主存储、Redis 为缓存
- [x] 新增 `apps/server/src/utils/redis.ts` 封装 Redis 客户端
- [x] 新增 API 路由：`GET /api/ai/chats`、`DELETE /api/ai/chat/:chatId`、`POST /api/ai/chat/:chatId/touch`
- [x] 更新 `GET /api/ai/chat/:chatId` 同时更新 `lastOpenedAt`
- [x] 新增 `apps/web/src/features/knowledge/types.ts` 的 `ChatSessionSummary` 类型
- [x] 新增 `apps/web/src/features/knowledge/utils.ts` 的 `getLastChatId` / `setLastChatId` 工具
- [x] 新增 `apps/web/src/hooks/use-chat-sessions.ts` 会话列表管理 hook
- [x] 更新 `apps/web/src/hooks/use-knowledge-chat.ts` 新增 `switchChat` / `loadMessages`
- [x] 新增 `apps/web/src/features/knowledge/components/chat-history-panel.tsx` 会话列表 UI
- [x] 更新 `apps/web/src/routes/_app/knowledge.tsx` 集成会话列表与切换
- [x] 更新 i18n 资源文件（en-US / zh-CN / ja-JP）
- [x] 更新单测 `apps/server/__tests__/ai-chat-store.test.ts`（26 个测试用例）
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
实现 Knowledge Chat 会话历史的服务端持久化（PostgreSQL + Redis 缓存）与前端可视化。
新增会话列表 UI，支持会话切换、新建、删除；刷新后自动恢复最近打开的会话。

### 2.11 空会话去重与自动清理

- [x] 在 `apps/server/src/services/ai-chat-store.ts` 新增 `isEmptySession` / `findRecentEmptySession` / `cleanupEmptySessions` 辅助函数
- [x] 更新 `createChat` 对空会话复用现有空会话、清理其余空会话
- [x] 更新 `listUserChats` 在列表查询时触发空会话清理（仅保留最近一条）
- [x] 新增 `deleteEmptyChat` 函数用于前端切换时清理
- [x] 新增 API 路由：`DELETE /api/ai/chat/:chatId/empty`
- [x] 更新 `apps/web/src/hooks/use-chat-sessions.ts` 新增 `deleteEmptyChat` / `isSessionEmpty`
- [x] 更新 `apps/web/src/hooks/use-chat-sessions.ts` 切换会话时自动清理前一空会话
- [x] 更新 `apps/web/src/routes/_app/knowledge.tsx` New Chat 避免重复创建空会话
- [x] 更新单测 `apps/server/__tests__/ai-chat-store.test.ts` 新增空会话管理用例
- [x] 运行 `pnpm run check-types` 确保无类型错误

Done:
实现空会话去重与自动清理功能。服务端在创建新空会话时复用现有空会话并清理重复项；前端切换会话时自动删除前一空会话。
New Chat 按钮在当前已是空会话时不再重复创建。

### 2.12 生成式 UI（Knowledge Chat 工具卡片）

- [x] 新增服务端工具与工具调用接入（天气 + 股票）
- [x] Web 端保留 UIMessage parts 并渲染工具卡片
- [x] 补充文档与最小单测

Done:
新增天气与股票工具并在 AI 流式接口按 provider 能力注入。
Web 端保留 UIMessage parts 渲染工具卡片，并补齐文档与最小单测。

---

## Phase 3: AI Summarize Entry（后续实现）

> 待 Phase 2 完成后再展开

---

## Phase 4: AI Suggest Review List（后续实现）

> 待 Phase 3 完成后再展开

---

## Phase 5: AI Usage / Token 记录（后续实现）

> 待 Phase 4 完成后再展开
